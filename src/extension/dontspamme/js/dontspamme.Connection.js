dontspamme.Connection = {
    is_authenticated: false,
    
    on_authenticated: function(self, success_callback, error_callback){
        if(){
            error_callback(self, error);
            return;
            
            self.is_authenticated = true;
            callback(self, response);
        }
        
    },
    

});